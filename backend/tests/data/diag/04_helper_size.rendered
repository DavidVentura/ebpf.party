error: possible out-of-bounds buffer access
   |
24 |     bpf_probe_read_kernel(buf, sz, &vals);
   |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ size can be [0, UINT32_MAX], while `buf` holds only 64 bytes
   |
help: narrow it first, e.g. `if (size > sizeof(buf)) return 0;`